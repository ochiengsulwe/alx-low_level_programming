#include <stdio.h>

/**
 * main - addresses of variables
 *
 * Return: Always 0
 *
 */
int main(void)
{
	char c;
	int n;

	printf("The address of variable 'c': %p \n", &c);
	printf("Address of varibale 'n' : %p \n", &n);
	return (0);
}
